- Change PrologixGPIBUSB backend to PyVISA.
- Make script files PeP compliant.
- Figure out how to access files while in use.  
- Main thread (GUI) and experiment thread require more rigirous locking for the alarms. Ex: If an alarm is triggered and you execute a command, the GUI will continue.
- Create a buffer for recent data in order to do in-situ analysis (ex: run a Jupyter notebook on a seperate thread that accesses the buffer). Consider using shared memory modude to handle this task.
